/**
 * @description 导出主题配置，注意事项：此配置下的项修改后需清理浏览器缓存！！！
 */
module.exports = {
  // 布局种类：分栏布局column
  layout: "column",
  // 分栏风格(仅针对分栏布局column时生效)：卡片风格card
  columnStyle: "card",
  // 是否固定头部固定
  fixedHeader: true,
  // 是否开启标签页
  showTabs: true,
  //是否默认收起左侧菜单
  collapse: false,
};
